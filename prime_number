#include<stdio.h>
int main()
{
	int n,i,flag=0;
	printf("enter number:");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(n%i==0)
		{
			flag=flag+1;
		}
	}
	if(flag==2)
	{
		printf("prime number");
	}
	else
	{
		printf("non prime");
	}
}
